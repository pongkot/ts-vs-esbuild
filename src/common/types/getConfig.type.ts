export type TGetConfig = {
    database: {
        acme: {
            user: string
            pass: string
            host: string
            name: string
            port: number
        }
    }
}
